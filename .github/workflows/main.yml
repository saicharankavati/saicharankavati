name: 🚀 Deploy website on push
'on': push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        run: echo "hello world"
      - name: 🚚 Get latest code1
        run: echo "hello w"
